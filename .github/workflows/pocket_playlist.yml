name: Pocket Playlist Generator

on:
  schedule:
    - cron: '*/15 * * * *' # Runs every 15 mins
  push:
    paths:
      - '**' # Runs on ANY file change
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Check out repository code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run Playlist Generator Script
        run: python create_pocket_playlist.py

      - name: Commit and Push Changes
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          
          # 1. Add the new playlist
          git add pocket_playlist.m3u
          
          # 2. COMMIT FIRST (This fixes the error 128)
          git commit -m "Auto-update Playlist" || echo "No changes to commit"
          
          # 3. NOW Pull the latest changes (like your youtube.txt edits)
          git pull origin main --rebase
          
          # 4. Push everything
          git push origin main
